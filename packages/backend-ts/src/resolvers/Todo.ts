import type { TodoResolvers } from "./types.generated";

export const Todo: TodoResolvers = {
  // For basic types like Todo, we often don't need custom resolvers
  // GraphQL will automatically resolve these fields from the returned object
};
